# 待整理笔记

### unity基本

* 游戏引擎的定义
  * 将一些游戏程序**必须的**，**通用的**，**稳定的**基础模块、中间件、制作工具、开发环境的封装&打包
  * 一个核心逻辑就是不断定时的按顺序执行特定逻辑

* unity优势在哪？
  * 上手容易
  * 组件化设计思想，扩展性强
  * 跨平台能力强大
* 弱点？
  * 不开源
  * 与UE比较渲染效率/效果较弱

* Unity资源组织的基本思想
  * 场景
  * 预制体
  * 代码资源
  * 美术资源

* Unity程序设计的基本思想----组件化
  * GameObject是容器，挂载Component
  * Component类：组件的基类
    * Behaviour类：继承自Component，查找删除挂载
      * MonoBehaviour：继承自Behaviour，拥有完整的**生命周期**
* Renderer
  * 场景中可以绘制的实体
  * 三种子类
    * MeshRender&MeshFliter
      * 最简单的网格模型
      * 其实就是如何绘制这些网格
    * skinedMeshRenderer
      * 蒙皮的（带骨骼动画的）模型
      * 3D图形学里常用的表现角色动画方式
        * 建立骨架层级，定义网格模型每个顶点收谷歌的位置影响的权重
        * 对骨骼做运动来去doing网格模型的运动
      * 通常是一个fbx文件
    * ParticleSystemRenderer
      * 粒子系统常用于特效表现（光、烟、火...)
      * 有一个发射器、发射固定数量的例子
* 关注游戏性能方面
  * ![企业微信截图_17654397841848](.\企业微信截图_17654397841848.png)

### 设计模式part

* #### 面向对象六大原则
  
  1. Single Responsibility Principle: 单一职责原则
     * 一个类只承担一个功能
       * 降低耦合性
       * 增强可读性与可维护性
       * 增加复用性
  2. Open/Close Principle: 开闭原则
     * 对扩展开放，对修改封闭
     * 子类重写父类代码
  3. Liskov Substitution Principle: 里氏替换原则
     * 继承逻辑正确性的表象：所有父类出现的地方，都可以用子类对象来替代
  4. Interface Segregation Principle: 接口隔离原则
     * 拆分接口
  5. Dependency Inversion Principle: 依赖倒置原则
     * 高层次的类不应该依赖低层次的类
       * 例如网络通信：游戏业务逻辑依赖TCP协议，在后期如果需要换成UDP类的接口。在设计之初游戏业务本身就该调用network这个类中的send/recv
  6. Law of Demeter: 迪米特法则
     * 最少依赖原则
     * 避免单个对象变化影响其他对象
       * 增强系统稳定性
       * 降低耦合度
  
* #### 创建型设计模式

* ##### 工厂方法模式

* 将类初始化/创建的工作交给另一个类

  *  要素：
    1. 抽象产品 - 例如weapon基类
    2. 具体产品 - 例如sward/gun子类
    3. 抽象工程 - 例如weaponFactory基类
    4. 具体工厂 - 例如swardFactory子类
  * 核心观念
    * 解耦客户端与具体类的依赖
      * 将具体代码与使用该产品的客户端内容进行分离

    * 提高系统的可扩展性
      * 在引入新产品的时候无需修改已有代码

    * 封装复杂的对象创建逻辑
      * 工厂内部屏蔽对象创建与初始化的复杂操作

    * 遵循开闭原则与单一之策原则
      * 提高可维护性
      * 支持更多产品组合

  * 应用场景
    * 日志系统、数据库驱动、文档生成

* ##### 抽象工厂模式

* 更多维度的工厂方法模式

* ##### 建造者模式

* 针对于复杂对象的分步构建，灵活配置

  * 要素：
    * 产品：如GameLevel类
    * 抽象生成器：如LevelBuilder类
    * 具体生成器：如EasyLevelBuilder类
    * 导演/主管：如GameLevelDirector类

  * 应用场景：
    * 角色创建系统
    * 地图关卡生成
    * UI界面构建
    * 道具/装备系统


* ##### 原型设计模式

* 一种创建型设计模式，通过复制现有对象来创建新对象，而非实例化

* 适用与频繁创建相似对象的场景

  * 要素：

    * Prototype抽象类型：声明克隆自身的接口

      * 

      ```c#
      // 抽象原型接口
      public interface IEnemyPrototype
      {
          IEnemyPrototype Clone();
          string GetInfo();
      }
      ```

    * 具体类型：实现克隆操作的具体类

      * 

      ```c#
      // 具体原型类 - 法师
      public class Mage : IEnemyPrototype
      {
          public string Name { get; set; }
          public int Mana { get; set; }
          public int SpellPower { get; set; }
          public string Element { get; set; }
      
          public Mage(string name, int mana, int spellPower, string element)
          {
              Name = name;
              Mana = mana;
              SpellPower = spellPower;
              Element = element;
          }
      
          public IEnemyPrototype Clone()
          {
              return new Mage(Name, Mana, SpellPower, Element);
          }
      
          public string GetInfo()
          {
              return $"法师: {Name}, 法力值: {Mana}, 法术强度: {SpellPower}, 元素: {Element}";
          }
      }
      ```

    * 创建工厂

      * ```c#
        // 敌人工厂（使用原型模式）
        public class EnemyFactory
        {
            private Dictionary<string, IEnemyPrototype> _prototypes = new Dictionary<string, IEnemyPrototype>();
        
            public EnemyFactory()
            {
                // 初始化原型对象
                _prototypes["mage"] = new Mage("基础法师", 200, 30, "火焰");
            }
        
            public IEnemyPrototype CreateEnemy(string type)
            {
                if (_prototypes.ContainsKey(type))
                {
                    return _prototypes[type].Clone();
                }
                throw new ArgumentException($"未知的敌人类型: {type}");
            }
        }
        
        // 使用示例
        class Program
        {
            static void Main()
            {
                EnemyFactory factory = new EnemyFactory();
                
                // 快速创建多个相同类型的敌人
                IEnemyPrototype mage1 = factory.CreateEnemy("mage");
                
                Console.WriteLine(mage1.GetInfo());   // 法师: 基础法师, 法力值: 200, 法术强度: 30, 元素: 火焰
            }
        } 
        ```

  * 核心观念

    * 很自由的复制一个对象，类可以自行决定需要复制哪些字段

* ##### 单例模式

* 所有的管理器都应该使用单例模式

* 日志管理器，数据库连接池等创建耗时，使用时消耗资源过多

* 饿汉模式

  * 在类加载时就使用静态成员变量创建了自己的实例

  * ```c++
    class Singleton {
        private:
        	//静态成员变量，实例化在类加载时进行
        	static Singleton instance;
        	
        	//私有化构造函数，禁止外部实例化
       		Singleton() {
                
            }
        
        	//禁止拷贝构造和赋值操作
        	Singelton(const Singleton&) = delete;
        	Singleton& operator=(const Singleton&) = delete;
        
        public:
        	//提供一个静态方法来访问实例
        	static Singleton& getInstance() {
                return instance;
            }
        
        	//其他方法......
    }
    ```

* 懒汉模式

  * 在类加载时，单例对象为空，在第一次调用时，类才会被创建

  * ```c++
    static Singleton* getInstance() {
    	if (instance == nullptr) {
            instance = new Singleton();
        }            
        return instance;
    }
    ```

  * 更加节省资源占用
  
  * 但是由于是动态创建的资源实例，在多线程环境下有安全隐患
  
    * 可能有两个实例同时创建
    * 需要加锁避免
  
* 单例系统的生命周期都应该是静态变量的生命周期（从创建起占据整个程序的生命周期）


  * 故而对单例系统额外提供的资源释放方法是不必要且累赘的


* #### 结构型设计模式

* ##### 适配器模式

* 用适配器接口，用一套代码处理不同对象的行为

* 应用场景

  * 处理不同设备的输入

  * 对接策划表格

* ##### 桥接模式

* ##### 组合模式

* ##### 装饰器模式

* ##### 外观模式

* ##### 享元模式

* ##### 代理模式



















### 活动功能

#### 根据权重的不放回抽奖

* 每次抽奖都先过滤一遍奖池保证保底数量

  * ```javascript
     let filteredPrizes = ChapterConfig.filter(p => parseInt(p.GiftNotGetCount) <= drawCount);
      
        console.log(`drawcount = ${drawCount}, 过滤后的奖品组包含 ${filteredPrizes.length} 个奖品.`);
      
    ```

* 算出所有奖池中物品的权重总和，生成一个在总权重以内的随机数

* 用生成的随机数不断地减去每个物品的权重直到小于0

* ```javascript
  let totalRewardValue = sumsUp(filteredPrizes, 'GiftWeight');
      if (isNaN(totalRewardValue) || totalRewardValue <= 0) {
          console.error(`总奖励值错误： ${totalRewardValue}`);
          return {
              errcode: myErrcode.Wrong_WEIGHT,
              errInfo: `总奖励值错误： ${totalRewardValue}`,
          };
      }
      
      let randNum = Math.floor(Math.random() * totalRewardValue);
  
      console.log(`奖品组的总权重为 ${totalRewardValue}，生成的随机数为 ${randNum}.`);
      for (let i = 0; i < filteredPrizes.length; i++) {
          randNum -= filteredPrizes[i].GiftWeight;
          if (randNum < 0) {
              prize = filteredPrizes[i];
              const index = ChapterConfig.findIndex(p => p.Id === prize.Id);
              if (index !== -1) {
                  ChapterConfig.splice(index, 1);
              }
              break;
          }
      }
  ```

* 











### 如何优雅的引用对象、脚本间沟通

#### 引用游戏对象的方法

* 尽量避免

  * ```c#
    GameObject.Find("character");
    GameObject.FindWithTag("TagName");
    GameObject.FindObjectsOfType<Unit>();
    ```

* 手动把游戏对象拖入

  * ```c#
    [SerializeField] private GameObject _prefab;
    private GameObject _spawnObj;
    void Start() {
        _spawnObject = Instantiate(_prefab);
    }
    ```

* 访问复数对象

  * ```c#
    public static Manager Instance;
    private List<Unit> Units {get; private set;} = new List<Unit>();
    //也可以写一个get/set
    
    void Awake() => Instance = this;
    ```

* 一个通信的好习惯

  * ```c#
    if(collision.gameObject.TryGetComponent(out HealOnTouch heal)) {
        heal.HealUnit();
    }
    ```

  * 













### 经验教训

* 客户端永远不要自己走逻辑调用服务器暴露的主动接口，所有情况需要经过服务器校验
* c#的类与ts的类不要去stringfy，既不能输出，如果其中有函数的话又可能会循环引用抛出异常，导致代码进行不下去
* 一定要仔细校验所有接口取到数据的存在与否，不要过于相信取出来的数据就直接用来赋值，可能会导致代码报错卡死，一定一定要记得判空
* 哪些地方的代码有风险最好自己心里有点数。。。不要自行编写逻辑，合理指出不合理需求

